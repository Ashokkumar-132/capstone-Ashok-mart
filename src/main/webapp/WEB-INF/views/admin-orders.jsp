<%@ page contentType="text/html; charset=UTF-8" pageEncoding="UTF-8" %><%@ taglib prefix="c" uri="http://java.sun.com/jsp/jstl/core" %><%@ taglib prefix="fmt" uri="http://java.sun.com/jsp/jstl/fmt" %><!DOCTYPE html><html lang="en"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>Orders | AshokMart</title><link rel="stylesheet" href="${pageContext.request.contextPath}/css/auth.css"><link rel="stylesheet" href="${pageContext.request.contextPath}/css/admin.css"></head><body><jsp:include page="/WEB-INF/views/includes/header.jsp"/><main class="admin-layout"><div class="admin-page-heading"><div><p class="eyebrow">Administration</p><h1>Order management</h1><p class="lede">Inspect buyer, seller, and historical line-item information.</p></div><a class="back-link" href="${pageContext.request.contextPath}/admin/dashboard">← Dashboard</a></div><section class="admin-panel"><form class="admin-filter-bar order-filter" method="get"><label>Search<input name="search" value="${searchTerm}" placeholder="Order ID, buyer, or email"></label><label>Status<select name="status"><option value="">All statuses</option><option value="PENDING" <c:if test="${selectedStatus == 'PENDING'}">selected</c:if>>PENDING</option><option value="CONFIRMED" <c:if test="${selectedStatus == 'CONFIRMED'}">selected</c:if>>CONFIRMED</option><option value="PROCESSING" <c:if test="${selectedStatus == 'PROCESSING'}">selected</c:if>>PROCESSING</option><option value="SHIPPED" <c:if test="${selectedStatus == 'SHIPPED'}">selected</c:if>>SHIPPED</option><option value="DELIVERED" <c:if test="${selectedStatus == 'DELIVERED'}">selected</c:if>>DELIVERED</option><option value="CANCELLED" <c:if test="${selectedStatus == 'CANCELLED'}">selected</c:if>>CANCELLED</option></select></label><button class="primary-button" type="submit">Apply filters</button></form><div class="admin-table-wrap"><table class="admin-table"><thead><tr><th>Order</th><th>Buyer</th><th>Date</th><th>Items</th><th>Total</th><th>Status</th><th></th></tr></thead><tbody><c:choose><c:when test="${empty orderPage.orders}"><tr><td colspan="7" class="admin-empty">No orders match these filters.</td></tr></c:when><c:otherwise><c:forEach items="${orderPage.orders}" var="order"><tr><td><strong>#${order.id}</strong></td><td>${order.buyerName}<small>${order.buyerEmail}</small></td><td>${order.createdAt}</td><td>${order.itemCount}</td><td><fmt:formatNumber value="${order.totalAmount}" type="currency" currencyCode="USD"/></td><td><span class="admin-status order-status-${order.status}">${order.status}</span></td><td><a href="${pageContext.request.contextPath}/admin/orders/view?id=${order.id}">View details</a></td></tr></c:forEach></c:otherwise></c:choose></tbody></table></div><div class="admin-pagination"><span>Page ${orderPage.currentPage} of ${orderPage.totalPages} · ${orderPage.totalResults} orders</span><span><c:if test="${orderPage.hasPrevious}"><a href="?page=${orderPage.currentPage - 1}&search=${searchTerm}&status=${selectedStatus}">Previous</a></c:if> <c:if test="${orderPage.hasNext}"><a href="?page=${orderPage.currentPage + 1}&search=${searchTerm}&status=${selectedStatus}">Next</a></c:if></span></div></section></main></body></html>
