<%@ page contentType="text/html; charset=UTF-8" pageEncoding="UTF-8" %>
<%@ taglib prefix="c" uri="http://java.sun.com/jsp/jstl/core" %>
<%@ taglib prefix="fn" uri="http://java.sun.com/jsp/jstl/functions" %>
<!DOCTYPE html>
<html lang="en"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0"><title>User Management | AshokMart</title><link rel="stylesheet" href="${pageContext.request.contextPath}/css/auth.css"><link rel="stylesheet" href="${pageContext.request.contextPath}/css/admin.css"></head>
<body><jsp:include page="/WEB-INF/views/includes/header.jsp" /><main class="admin-layout"><div class="admin-page-heading"><div><p class="eyebrow">Administration</p><h1>User Management</h1><p class="lede">Manage buyer and seller account access without exposing authentication data.</p></div><a class="secondary-button" href="${pageContext.request.contextPath}/admin/dashboard">Dashboard</a></div><c:if test="${not empty adminUserSuccess}"><div class="form-message success">${adminUserSuccess}</div></c:if><c:if test="${not empty adminUserError}"><div class="form-message error">${adminUserError}</div></c:if><section class="admin-panel"><form class="admin-filter-bar" method="get" action="${pageContext.request.contextPath}/admin/users"><label>Search name or email<input type="search" name="q" value="${search}" maxlength="100" placeholder="Search users"></label><label>Role<select name="role"><option value="ALL">All roles</option><option value="BUYER" ${roleFilter == 'BUYER' ? 'selected' : ''}>Buyer</option><option value="SELLER" ${roleFilter == 'SELLER' ? 'selected' : ''}>Seller</option><option value="ADMIN" ${roleFilter == 'ADMIN' ? 'selected' : ''}>Admin</option></select></label><label>Status<select name="status"><option value="ALL">All status</option><option value="ACTIVE" ${statusFilter == 'ACTIVE' ? 'selected' : ''}>Active</option><option value="INACTIVE" ${statusFilter == 'INACTIVE' ? 'selected' : ''}>Inactive</option></select></label><button class="primary-button" type="submit">Apply filters</button></form><c:choose><c:when test="${empty userPage.users}"><div class="admin-empty">No users match the selected filters.</div></c:when><c:otherwise><div class="admin-table-wrap"><table class="admin-table"><caption class="sr-only">Registered AshokMart accounts</caption><thead><tr><th>User</th><th>Email</th><th>Role</th><th>Registered</th><th>Status</th><th>Action</th></tr></thead><tbody><c:forEach items="${userPage.users}" var="user"><tr><td><div class="admin-user-name"><strong>${user.name}</strong><span>ID ${user.id}</span></div></td><td>${user.email}</td><td><span class="admin-badge ${fn:toLowerCase(user.role)}">${user.role}</span></td><td>${user.createdAt}</td><td><span class="admin-status ${user.enabled ? 'active' : 'inactive'}">${user.enabled ? 'Active' : 'Inactive'}</span></td><td><c:choose><c:when test="${user.role == 'ADMIN' && user.id == sessionScope.authenticatedUser.userId}"><span class="admin-protected">Current admin</span></c:when><c:when test="${user.enabled}"><form class="admin-status-form" method="post" action="${pageContext.request.contextPath}/admin/users/deactivate"><input type="hidden" name="userId" value="${user.id}"><button class="danger" type="submit">Deactivate</button></form></c:when><c:otherwise><form class="admin-status-form" method="post" action="${pageContext.request.contextPath}/admin/users/activate"><input type="hidden" name="userId" value="${user.id}"><button type="submit">Activate</button></form></c:otherwise></c:choose></td></tr></c:forEach></tbody></table></div><div class="admin-pagination"><c:if test="${userPage.hasPrevious}"><c:url var="previousUrl" value="/admin/users"><c:param name="q" value="${search}"/><c:param name="role" value="${roleFilter}"/><c:param name="status" value="${statusFilter}"/><c:param name="page" value="${userPage.page - 1}"/></c:url><a class="secondary-button" href="${previousUrl}">Previous</a></c:if><span>Page ${userPage.page} of ${userPage.totalPages} · ${userPage.totalUsers} users</span><c:if test="${userPage.hasNext}"><c:url var="nextUrl" value="/admin/users"><c:param name="q" value="${search}"/><c:param name="role" value="${roleFilter}"/><c:param name="status" value="${statusFilter}"/><c:param name="page" value="${userPage.page + 1}"/></c:url><a class="secondary-button" href="${nextUrl}">Next</a></c:if></div></c:otherwise></c:choose></section></main></body></html>
