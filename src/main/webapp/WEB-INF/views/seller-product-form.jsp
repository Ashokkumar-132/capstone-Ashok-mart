<%@ page contentType="text/html; charset=UTF-8" pageEncoding="UTF-8" %>
<%@ taglib prefix="c" uri="http://java.sun.com/jsp/jstl/core" %>
<!DOCTYPE html>
<html lang="en"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0"><title>${editing ? 'Edit product' : 'Add product'} | AshokMart</title><link rel="stylesheet" href="${pageContext.request.contextPath}/css/auth.css"><link rel="stylesheet" href="${pageContext.request.contextPath}/css/seller.css"></head>
<body><jsp:include page="/WEB-INF/views/includes/header.jsp" /><main class="seller-layout form-layout"><c:choose><c:when test="${formNotFound}"><section class="seller-empty seller-panel"><p class="eyebrow">Product unavailable</p><h1>We could not find that product.</h1><p>The product may not belong to this seller account.</p><a class="secondary-button" href="${pageContext.request.contextPath}/seller/products">Back to products</a></section></c:when><c:otherwise><div class="seller-page-heading"><div><p class="eyebrow">Seller workspace</p><h1>${editing ? 'Edit product' : 'Add a product'}</h1><p class="lede">${editing ? 'Refresh the details your customers see.' : 'Add a polished product to your storefront.'}</p></div><a class="back-link" href="${pageContext.request.contextPath}/seller/products">← Cancel</a></div><section class="seller-panel seller-form-panel"><c:if test="${not empty productError}"><div class="form-message error" role="alert">${productError}</div></c:if><form method="post" action="${pageContext.request.contextPath}/seller/products/${editing ? 'update' : 'create'}" class="seller-form"><c:if test="${editing}"><input type="hidden" name="id" value="${product.id}"></c:if><div class="form-field"><label for="name">Product name <span class="required">*</span></label><input id="name" name="name" type="text" maxlength="255" value="${product.name}" required></div><div class="form-field"><label for="description">Description</label><textarea id="description" name="description" maxlength="4000" rows="5">${product.description}</textarea></div><div class="seller-form-grid"><div class="form-field"><label for="categoryId">Category <span class="required">*</span></label><select id="categoryId" name="categoryId" required><option value="">Choose a category</option><c:forEach items="${categories}" var="category"><option value="${category.id}" ${category.id == product.categoryId ? 'selected' : ''}>${category.name}</option></c:forEach></select></div><div class="form-field"><label for="price">Price (USD) <span class="required">*</span></label><input id="price" name="price" type="number" min="0" step="0.01" value="${product.price}" required></div><div class="form-field"><label for="stockQuantity">Stock quantity <span class="required">*</span></label><input id="stockQuantity" name="stockQuantity" type="number" min="0" step="1" value="${product.stockQuantity}" required></div><div class="form-field"><label for="imageUrl">Image URL/path</label><input id="imageUrl" name="imageUrl" type="text" maxlength="1000" value="${product.imageUrl}" placeholder="https://… or /images/product.jpg"></div></div><div class="seller-form-actions"><a class="secondary-button" href="${pageContext.request.contextPath}/seller/products">Cancel</a><button class="primary-button seller-small-button" type="submit">${editing ? 'Save changes' : 'Create product'}</button></div></form></section></c:otherwise></c:choose></main></body></html>
