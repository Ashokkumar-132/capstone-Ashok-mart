<%@ page contentType="text/html; charset=UTF-8" pageEncoding="UTF-8" %>
<%@ taglib prefix="c" uri="http://java.sun.com/jsp/jstl/core" %>
<%@ taglib prefix="fmt" uri="http://java.sun.com/jsp/jstl/fmt" %>
<!DOCTYPE html>
<html lang="en"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0"><title>My Products | AshokMart</title><link rel="stylesheet" href="${pageContext.request.contextPath}/css/auth.css"><link rel="stylesheet" href="${pageContext.request.contextPath}/css/seller.css"></head>
<body><jsp:include page="/WEB-INF/views/includes/header.jsp" /><main class="seller-layout"><div class="seller-page-heading"><div><p class="eyebrow">Seller workspace</p><h1>My Products</h1><p class="lede">Only products owned by your seller account appear here.</p></div><div class="seller-heading-actions"><a class="secondary-button" href="${pageContext.request.contextPath}/seller/dashboard">Dashboard</a><a class="primary-button" href="${pageContext.request.contextPath}/seller/products/new">+ Add product</a></div></div>
<c:if test="${not empty sellerSuccess}"><div class="form-message success">${sellerSuccess}</div></c:if><c:if test="${not empty sellerError}"><div class="form-message error">${sellerError}</div></c:if>
<c:choose><c:when test="${empty products}"><section class="seller-empty seller-panel"><h2>No products yet.</h2><p>Add a product to start building your storefront.</p><a class="primary-button seller-small-button" href="${pageContext.request.contextPath}/seller/products/new">Create product</a></section></c:when><c:otherwise><section class="seller-panel seller-table-panel"><div class="seller-table-wrap"><table class="seller-table"><caption class="sr-only">Products owned by your seller account</caption><thead><tr><th>Product</th><th>Category</th><th>Price</th><th>Stock</th><th>Status</th><th>Actions</th></tr></thead><tbody><c:forEach items="${products}" var="product"><tr><td><div class="table-product"><div class="seller-thumb"><c:choose><c:when test="${not empty product.imageUrl}"><img src="${product.imageUrl}" alt="${product.name}"></c:when><c:otherwise><span>AM</span></c:otherwise></c:choose></div><strong>${product.name}</strong></div></td><td><c:forEach items="${categories}" var="category"><c:if test="${category.id == product.categoryId}">${category.name}</c:if></c:forEach></td><td><fmt:formatNumber value="${product.price}" type="currency" currencyCode="USD" /></td><td>${product.stockQuantity}</td><td><span class="seller-state ${product.active ? 'active' : 'inactive'}">${product.active ? 'Active' : 'Inactive'}</span></td><td><div class="table-actions"><a class="text-button" href="${pageContext.request.contextPath}/seller/products/edit?id=${product.id}">Edit</a><form method="post" action="${pageContext.request.contextPath}/seller/products/${product.active ? 'deactivate' : 'activate'}"><input type="hidden" name="id" value="${product.id}"><button class="text-button danger-button" type="submit">${product.active ? 'Deactivate' : 'Activate'}</button></form></div></td></tr></c:forEach></tbody></table></div><p class="seller-note">Products are deactivated instead of hard-deleted so cart, order, and review history remains intact.</p></section></c:otherwise></c:choose></main></body></html>
